var character = (function () {
	function Character(){
		//CONSTANTS(?)
		thi.RADIUS;
		this.TIER_HEALTH = [];
		
		//Variables that are public
		this.x;
		this.y;
		this.color;
		this.tier;

		//Variables that Bobby doesn't know how to make private
		var xspeed, yspeed, health;
		
	}


	/**
	 * Updates the characters 
	 * @param {String} 
	 * @param {Object{string, boolean}}
	 * @returns void
	 */
	Character.prototype.update = function( string, Object { string, boolean}) {
		
		this.update();
	}

	/**
	 * Draw the characters on the screen
	 * @param {CanvasRenderingContext2D} - context on which the character is drawn
	 * @returns void
	 */
	Character.prototype.draw = function draw(ctx) {
		
		
	}

})();